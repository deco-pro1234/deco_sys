# 财务收支记录程序 - 详尽开发手册 (Development Manual)

## 1. 项目概述 (Project Overview)
本项目是一个侧重于手机端使用的 H5 财务收支记录程序。页面要求自适应、排版清晰简洁，高信息密度，符合“实用为先”的原则。系统分为普通用户与管理员两种权限，核心功能包括：财务数据录入（收入/支出）、资金池管理、分类管理、附件管理、用户管理以及多维度的财务报表生成与 PDF 导出。
**代码规范**：所有程序代码必须加上详细的中文简体注释。

## 2. 技术栈建议 (Tech Stack Recommendation)
为了实现“无需介入即可独立完成开发”的目标，建议采用全栈框架以简化部署和开发流程：
*   **前端框架**：Next.js (React) - 支持 H5 响应式，服务端渲染与 API 路由无缝衔接。
*   **UI 样式**：Tailwind CSS - 便于实现极简、紧凑的移动端优先排版 (追求高資訊密度、手機優先、iOS 質感)。
*   **数据库**：SQLite (配合 Prisma ORM) - 零配置，便于初期本地开发与后期迁移。
*   **附件存储**：本地文件系统 (初期) 或轻量级云存储 (后期)。前端使用 `browser-image-compression` 进行图片压缩。
*   **图表与 PDF/导出功能**：`jspdf` + `jspdf-autotable` (报表生成)，`jszip` (多文件打包下载)。

## 1.4 開發日誌 (Development Log)
- **[2026-08-12] MVP 核心功能完成**: 完成本地 SQLite 版本的五大核心表設計，實現收支紀錄、分類管理、資金池、管理員權限與報表導出功能。
- **[2026-08-13] UI 升級與優化**: 全面升級為 Apple iOS 設計風格（圓角、淺灰背景、無邊框設計）。修復手機端版面溢出問題。
- **[2026-08-13] PDF 報表強化**: 修復中文亂碼（引入 Noto Sans SC 字體），首頁加入 HKD/RMB 獨立資產統計卡片與彩色進度條 (Bar Chart) 分類統計。
- **[2026-08-13] 體驗優化**: 加入全站頂部狀態列（顯示當前登入角色與登出按鈕）；修復表單提交後首頁數據（總資產與列表）未即時更新的問題。
- **[2026-08-13] 雲端部署 (Railway)**: 將資料庫由本地 SQLite 遷移至雲端 PostgreSQL，解決 Railway 部署時的 502 Bad Gateway 與 Volume 持久化遺失問題，系統成功上線。
- **[2026-09-06] 審核附件修復**: 「查看附件」改為 Blob URL 開啟，修復 Chrome 攔截 `data:` 頂層導航導致空白頁。
- **[2026-09-06] 報表中心強化**:
  - 收支明細 PDF 改為 A4 橫向，欄位含主/子/孫分類、資金池、附件數。
  - 新增活動報表、合約報表分頁與列表 PDF 匯出。
  - 會計結算包重構：`00_憑證索引.csv` + `01` 正式摘要/分類/橫向總表 PDF + `02_憑證/`（`V001_日期_類型_分類_金額.ext` 可讀命名）；憑證號與檔名對照。
  - 匯出語文可選繁體中文 / English（獨立於介面語言）。
  - 報表頁 UI 加寬與分頁風格優化。
- **[2026-09-06] 合約可編輯（建立者權限）**:
  - 新增 `updateContract`：建立者可編輯標題、類型、金額、生效/到期日、提醒天數、資金池、備註，並可刪除合約。
  - 其他管理員僅可新增附件與備註（唯讀核心欄位）。
  - 詳情彈窗依 `userId === currentUserId` 顯示儲存/刪除；非建立者顯示權限提示。
- **[2026-09-06] Resend 到期郵件提醒**:
  - 新增 `ReminderEmailLog` 去重表；Cron API `/api/cron/reminders`（`CRON_SECRET`）。
  - 收件人：`REMINDER_EMAILS`；發送：Resend（`RESEND_API_KEY` / `RESEND_FROM`）。
  - 範圍：全部合約 + 公開活動；時區 Asia/Hong_Kong。
  - 觸發：提前提醒日、到期前 5 天、到期當天、過期第 1/7/30 天。
- **[2026-09-06] 常駐 cron-worker（node-cron）**:
  - 新增 [`cron-worker/`](./cron-worker/)：不依賴 cron-job.org；Railway 另開一台服務，Root=`cron-worker`。
  - 預設每天香港 09:00 `POST` 網站 `/api/cron/reminders`；**不要**在網站服務開 Railway Cron Schedule。
- **[2026-09-14] WhatsApp Webhooks**:
  - 端點 `GET/POST /api/webhooks/whatsapp`（Meta 驗證 + 入站訊息）。
  - 文字指令：幫助／提醒／最近／分類／資金池／報表／公帳入數（需確認）。
  - `WhatsAppBinding` 綁定表 + 管理後台「WhatsApp」分頁；可選到期 WhatsApp 推播。
  - 說明：[`docs/whatsapp-webhooks.md`](./docs/whatsapp-webhooks.md)。
- **[2026-09-14] WhatsApp 分步公帳＋OCR（已拍板，待 Meta 審核後開發）**:
  - 流程：`公帳`／傳圖 → 類型按鈕 → 分類 List → 金額／備註／附件 → 是否 OCR → 確認入帳。
  - 手填優先、OCR 補缺；確認前不寫庫。
  - 規格與 checklist：[`docs/whatsapp-interaction-roadmap.md`](./docs/whatsapp-interaction-roadmap.md) §4.1。

### 1.5 部署到 Railway 相關問題與修復
*   **Next.js 靜態打包錯誤 (Prerender Error)**：Next.js 在 `npm run build` 時會嘗試預渲染頁面，導致存取資料庫失敗。
    *   **解決方案**：在 `layout.tsx` 中加入 `export const dynamic = "force-dynamic";` 強制全站動態渲染。
*   **Railway Nixpacks 網路超時 (mise timeout)**：Railway 預設的建置工具在下載底層依賴時發生 `502 Bad Gateway`。
    *   **解決方案**：改為提供 `Dockerfile`，強制使用 Docker 引擎進行打包，完全繞過 Nixpacks 的網路問題。同時修改 `next.config.ts` 啟用 `standalone` 輸出模式。
*   **PostgreSQL 資料表未建立 (The table does not exist)**：切換到 PostgreSQL 後，資料庫為空，導致查詢失敗。
    *   **解決方案**：在 Dockerfile 的啟動指令 (`CMD`) 中加入 `npx prisma db push --accept-data-loss`，確保每次容器啟動時自動同步 Prisma Schema 到資料庫。

## 3. 核心数据模型 (Data Models)
1.  **用户表 (User)**：密码 (作为唯一登录凭证，明文存储/展示)、角色名称、是否管理员、资金池启用状态。
2.  **分类表 (Category)**：分类名称、父分类 ID (支持两级，删除时相关记录归入“未分类”)。
3.  **资金池表 (CapitalPool)**：名称、HKD 余额、RMB 余额、关联角色 ID (如适用)。
4.  **收支记录表 (Record)**：日期、收支类型(收入/支出)、分类、子分类、备注、币种(HKD/RMB)、金额(+/-)、资金池、附件URL、操作人。
5.  **附件表 (Attachment)**：文件路径、大小、上传时间、上传角色、所属分类。

## 4. 页面与功能结构说明 (Page & Feature Specifications)

### 4.1 登录页面 (Login Page)
*   **功能**：提供“普通登录”和“管理员登录”切换（默认普通登录）。
*   **逻辑**：无需账号，仅提供一个“密码”输入框。密码匹配用户白名单即可登录。

### 4.2 管理页面 (Admin Page)
*仅管理员可访问，包含四个标签分页：*
1.  **分类管理**：
    *   两级树状列表（可展开/收起）。
    *   支持新增、修改名称、删除（修改和删除需二次确认弹窗）。
    *   **业务逻辑**：删除分类后，数据库中引用该分类的条目自动归入“未分类”。
2.  **附件管理**：
    *   列表显示最近上传的附件。
    *   列信息：时间、文件大小、上传角色、所属分类。
3.  **资金池管理**：
    *   列表显示资金池名称、HKD 累计余额、RMB 累计余额。
    *   支持新增、修改名称、删除。
4.  **用户管理**：
    *   创建白名单密码（明文显示），可修改/删除，可添加角色名称。
    *   **资金池联动**：包含“启用/禁用”资金池按钮。启用时，在“资金池管理”自动创建该角色名的资金池；禁用时，资金池列表内该项及历史金额置灰显示。

### 4.3 个人主页 (Personal Home - 登录后默认页)
*   **Hero 区域**：显示个人名下 HKD 和 RMB 累计总数。旁边设“报表”按钮跳转至报表页。
*   **入数卡片区域 (双标签页：收入/支出)**：
    *   *视觉区分*：收入模板底色淡蓝，支出模板底色淡红。
    *   *日期*：必选，默认当天，支持日历选择。
    *   *分类*：必选，下拉列表，底部带“新增”选项（弹窗输入名称，确定后即刻可选）。
    *   *子分类*：如父分类有子分类则必选；无则显示“无”。底部同样带“新增”选项。
    *   *备注说明*：选填，默认灰色提示“选填”。
    *   *币种*：必选，HKD 或 RMB 单选。
    *   *金额*：必填。收入强制正数，支出强制负数。
    *   *资金池*：必选，下拉列表默认自己，其余来自资金池管理。
    *   *上传附件*：限 1 张图片，前端压缩至 200K 以内。上传成功显示大小，按钮变为“删除”。
    *   *提交按钮*：蓝色“提交收入” / 红色“提交支出”。点击后弹出信息简述确认框。下方“再次确认”按钮带 **5秒倒计时**，倒计时结束后才可点击。
*   **快速浏览列表**：
    *   位于模板最下方，实时更新显示当前角色最近提交的 10 条记录（新到旧）。

### 4.4 报表页面 (Report Page)
*仅管理员可访问。*
*   **分頁**：收支報表 / 活動報表 / 合約報表。
*   **筛选条件**：
    *   *日期*：開始 / 結束日期（收支依記錄日；活動依活動日；合約依生效日）。
    *   *分類 / 資金池 / 角色 / 狀態*：依分頁顯示對應篩選。
    *   *關鍵字*：備註或標題模糊搜尋。
*   **匯出語文**：可選「繁體中文」或「English」。只影響 PDF / CSV / ZIP 內容與檔名，不改變頁面介面語言。
*   **导出功能**：
    1.  `匯出明細列表 PDF`（收支）：A4 橫向總表，含主/子/孫分類、資金池、附件數等。
    2.  `匯出會計結算包`（收支）：ZIP 內含
        - `00_憑證索引.csv`（Excel 可開）
        - `01_會計結算報表_*.pdf`（封面摘要、分類統計、橫向明細總表，含憑證號）
        - `02_憑證/`（可讀檔名：`V001_YYYY-MM-DD_EX_分類_金額.ext`）
    3.  `匯出活動列表 PDF` / `匯出合約列表 PDF`：橫向列表，欄位含附件數等。

### 4.5 合約頁面 (Contracts Page)
*仅管理员可访问。*
*   **新增合約**：標題、收入/支出、生效日、到期日、提醒天數、金額、資金池、備註、附件。
*   **詳情與權限**：
    *   *建立者*：可編輯上述核心欄位並儲存；可刪除合約。
    *   *其他管理員*：核心欄位唯讀；可新增附件與備註。
*   **到期提醒**：依提醒天數分組顯示已過期 / 今天 / 即將到期。
*   **郵件提醒（Resend）**：
    *   環境變數（**網站服務** Sk11_finance）：`RESEND_API_KEY`、`RESEND_FROM`、`REMINDER_EMAILS`、`CRON_SECRET`、`APP_BASE_URL`。
    *   **收件人**：`REMINDER_EMAILS` 必須是真實信箱（如 `you@gmail.com`）。**不要**填 `onboarding@resend.dev`（那是發件人沙盒位址，不是收件人）。
    *   端點：`POST /api/cron/reminders`（Bearer `CRON_SECRET`）。
    *   觸發點（在「提前提醒天數」窗口內）：到期前 **30 / 15 / 7 / 3 / 1** 天、到期當天、過期第 1/7/30 天；自訂 `reminderDays` 若不在上述列表則另加一封；`ReminderEmailLog` 防重發。
    *   標題會依急迫性加前綴（提前提醒 → 請留意 → 即將到期 → 緊急 → 今天到期/已逾期）。
    *   建立／更新公開活動或合約時，若當天剛好是觸發日會立刻補寄。
    *   僅公開活動會進入郵件掃描；私人活動不發。
    *   **常駐 worker**：倉庫 `cron-worker/`（node-cron），Railway 另開服務、Root Directory=`cron-worker`；預設香港每日 09:00 打網站 API。測完請把 `RUN_ON_START` 設回 `false`。網站服務與 worker **都不要**開 Railway Cron Schedule。

## 5. UI/UX 规范 (UI/UX Guidelines)
*   **布局**：H5 手机端自适应，避免过度装饰，追求高信息密度。
*   **列表**：极小化列表行高与卡片高度，一屏展示尽可能多的信息。
*   **交互**：重要操作（删除、修改、提交财务数据）必须包含带防呆机制（如倒计时）的二次确认弹窗。

## 6. 自动化开发路径规划 (Autonomous Execution Plan)
本手册已作为后续 Agent 自主开发的基础。开发将遵循“Batch-based”小步快跑原则：
*   **Phase 1**：项目初始化 (Next.js + Tailwind + SQLite/Prisma)。
*   **Phase 2**：数据库 Schema 定义与核心 API 路由构建。
*   **Phase 3**：认证模块与管理页面 (分类、用户、资金池、附件) 开发。
*   **Phase 4**：个人主页开发 (Hero 数据、收支表单组件、前端压缩、倒计时提交)。
*   **Phase 5**：报表页面与 PDF 导出/打包下载功能。
*   **Phase 6**：全链路测试与 UI 细节打磨。