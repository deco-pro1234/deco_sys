{
  "name": "sk11-reminder-cron-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Always-on worker: daily reminder emails via node-cron (no external cron SaaS)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "node-cron": "^4.2.1"
  }
}
