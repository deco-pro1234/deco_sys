# Railway config when this service Root Directory = cron-worker
# (or Config File Path = /cron-worker/railway.toml).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
